# Python dependencies for the AutoLabel SAM2 backend (sam_service.py).
#
# The SAM2 *source code* is vendored as a git submodule at third_party/sam2 and
# imported via sys.path -- so it is NOT pip-installed here (that would try to
# build an optional CUDA C-extension we don't need on CPU). These are SAM2's
# runtime dependencies.
#
# torch is intentionally NOT listed: a working torch is assumed already present
# (a CPU build is fine). Installing it here could disturb an existing install.
# SAM2 needs torch >= 2.5.1.
#
#   pip install -r requirements.txt
#
hydra-core>=1.3.2
iopath>=0.1.10
pillow>=9.4.0
numpy
